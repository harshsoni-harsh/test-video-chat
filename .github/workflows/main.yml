name: Threat Modeling
on: [push, pull_request, workflow_dispatch]

jobs:
  threat-model:
    runs-on: ubuntu-latest
    steps:
      - name: Use CI Threat Modeler
        uses: harshsoni-harsh/threat-modeler@main
        with:
          api_key: ${{ secrets.AUTOTHREAT_API_KEY }}